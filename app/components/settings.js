import { View, Text } from "react-native";
import header from "../components/header.js";

export default function Settings(onSettings, ...props) {
    return (
        <View>
            <Text>No settings defined yet.</Text>
        </View>
    )
}